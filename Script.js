### JavaScript (script.js) ```javascript document.addEventListener('DOMContentLoaded', () => { const startButton = document.getElementById('startButton'); const dialContainer = document.querySelector('.dial-container'); const dial = document.querySelector('.dial'); let isRunning = false; // Initial state: not running startButton.addEventListener('click', () => { isRunning = !isRunning; // Toggle the state if (isRunning) { // Change button to "Stop" state startButton.textContent = 'Stop'; startButton.style.backgroundColor = '#f44336'; // Red for Stop startButton.style.boxShadow = '0 4px 8px rgba(244, 67, 54, 0.3)'; // Red shadow // Show and start spinning the dial dialContainer.classList.add('active'); dial.classList.add('spinning'); } else { // Change button to "Start" state startButton.textContent = 'Start'; startButton.style.backgroundColor = '#61dafb'; // Blue for Start startButton.style.boxShadow = '0 4px 8px rgba(0, 0, 0, 0.15)'; // Default shadow // Hide and stop spinning the dial dialContainer.classList.remove('active'); dial.classList.remove('spinning'); } }); }); ``` --- ### How to Use: 1. **Save the files:** * Save the HTML code as `index.html`. * Save the CSS code as `style.css` in the *same folder* as `index.html`. * Save the JavaScript code as `script.js` in the *same folder* as `index.html`. 2. **Open `index.html`** in your web browser. You'll see a "Start" button. Click it, and it will change to "Stop" and a blue dial will fade in and start spinning. Click it again, and it will revert to "Start" and the dial will fade out. This provides a clear visual and interactive feedback for the start/stop functionality!
You: .
LLM: Hello there! How can I help you today?
Type your message...
Send
